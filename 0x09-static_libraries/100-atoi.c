#include "main.h"

/**
 * _atoi - Entry point
 * @s: character to check
 * Description: 'Check if a character is uppercase'
 * Return: Always 0 (Success)
 */

int _atoi(char *s)
{
	if (*s)
	{
		return (1);
	}
	return (0);
}
